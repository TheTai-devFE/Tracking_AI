# PostgreSQL connection. Percent-encode special characters in the password.
DATABASE_URL=postgresql+psycopg://tracking_user:change_me@localhost:5432/tracking_ai

# Comma-separated frontend origins. Do not use * in a public deployment.
TRACKING_CORS_ORIGINS=http://localhost:3000
